.container
   .col-md-10
      = form_for [:admin, @role] do |f|
         %label Role Name :
         %br/
         = f.text_field :name, class: "form-control"
         %br/
         = f.submit "Submit", class: "btn-primary"